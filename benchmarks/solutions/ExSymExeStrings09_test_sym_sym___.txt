************************************

------------------------------------
MAS:
string analysis: SPC # = 2
(b_2_SYMSTRING startswith CONST_b) && (a_1_SYMSTRING startswith CONST_abc)
NPC constraint # = 0
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
Calling MAS

The smtlib query is: 
(declare-const a_1 String)
(declare-const b_2 String)

(assert (str.prefixof "b" b_2))
(assert (str.prefixof "abc" a_1))


=======================================

################################################
################################################
################	CACHE HIT	################
################################################
################################################

sat,
a_1: "abc"
b_2: "b"

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (not (str.prefixof "b" b_2)))
(assert (not (str.prefixof "abc" a_1)))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abB"
b_2 => "A"

MAS:
## Warning: empty path condition
### PCs: total:5 sat:5 unsat:0

string analysis: SPC # = 2
(b_2_SYMSTRING startswith CONST_b) && (a_1_SYMSTRING notstartswith CONST_abc)
NPC constraint # = 0
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
Calling MAS

The smtlib query is: 
(declare-const a_1 String)
(declare-const b_2 String)

(assert (str.prefixof "b" b_2))
(assert (not (str.prefixof "abc" a_1)))


=======================================

################################################
################################################
################	CACHE HIT	################
################################################
################################################

sat,
a_1: ""
b_2: "b"

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (not (= a_1 b_2)))
(assert (str.prefixof "b" b_2))
(assert (not (str.prefixof "abc" a_1)))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abA"
b_2 => "b"

MAS:
## Warning: empty path condition
### PCs: total:7 sat:7 unsat:0

string analysis: SPC # = 3
(a_1_SYMSTRING equals b_2_SYMSTRING) && (b_2_SYMSTRING startswith CONST_b) && (a_1_SYMSTRING notstartswith CONST_abc)
NPC constraint # = 0
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
Calling MAS

The smtlib query is: 
(declare-const a_1 String)
(declare-const b_2 String)

(assert (= a_1 b_2))
(assert (str.prefixof "b" b_2))
(assert (not (str.prefixof "abc" a_1)))


=======================================
Cache Hit Invalid reason(s):
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 1 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 1 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
---------------------------------------------------------------------
Using Alphabet: 0abc
Using bound: 4

************************************
(declare-const a_1 String)

(assert (not (str.prefixof "abc" a_1)))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abA"

MAS:
sat,
a_1: ""

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (not (str.prefixof "b" b_2)))
(assert (str.prefixof "abc" a_1))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abc"
b_2 => "A"

MAS:
## Warning: empty path condition
### PCs: total:12 sat:12 unsat:0

string analysis: SPC # = 2
(b_2_SYMSTRING startswith CONST_b) && (a_1_SYMSTRING startswith CONST_abc)
NPC constraint # = 0
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
Calling MAS

The smtlib query is: 
(declare-const a_1 String)
(declare-const b_2 String)

(assert (str.prefixof "b" b_2))
(assert (str.prefixof "abc" a_1))


=======================================

################################################
################################################
################	CACHE HIT	################
################################################
################################################

sat,
a_1: "abc"
b_2: "b"

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (= a_1 b_2))
(assert (not (str.prefixof "b" b_2)))
(assert (str.prefixof "abc" a_1))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abc"
b_2 => "abc"

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (= a_1 b_2))
(assert (str.prefixof "b" b_2))
(assert (not (str.prefixof "abc" a_1)))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "bA"
b_2 => "bA"

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (not (= a_1 b_2)))
(assert (str.prefixof "b" b_2))
(assert (str.prefixof "abc" a_1))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abc"
b_2 => "b"

MAS:
## Warning: empty path condition
### PCs: total:14 sat:14 unsat:0

string analysis: SPC # = 3
(a_1_SYMSTRING equals b_2_SYMSTRING) && (b_2_SYMSTRING startswith CONST_b) && (a_1_SYMSTRING startswith CONST_abc)
NPC constraint # = 0
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
Calling MAS

The smtlib query is: 
(declare-const a_1 String)
(declare-const b_2 String)

(assert (= a_1 b_2))
(assert (str.prefixof "b" b_2))
(assert (str.prefixof "abc" a_1))


=======================================
Cache Hit Invalid reason(s):
	INVALID due to 1 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 1 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 1 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
	INVALID due to 1 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
---------------------------------------------------------------------
Using Alphabet: 0abc
Using bound: 4

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (str.prefixof "b" b_2))
(assert (str.prefixof "abc" a_1))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abc"
b_2 => "b"

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (not (= a_1 b_2)))
(assert (not (str.prefixof "b" b_2)))
(assert (not (str.prefixof "abc" a_1)))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abB"
b_2 => "A"

MAS:
## Warning: empty path condition
### PCs: total:4 sat:4 unsat:0

string analysis: SPC # = 3
(a_1_SYMSTRING equals b_2_SYMSTRING) && (b_2_SYMSTRING notstartswith CONST_b) && (a_1_SYMSTRING notstartswith CONST_abc)
NPC constraint # = 0
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
Calling MAS

The smtlib query is: 
(declare-const a_1 String)
(declare-const b_2 String)

(assert (= a_1 b_2))
(assert (not (str.prefixof "b" b_2)))
(assert (not (str.prefixof "abc" a_1)))


=======================================
Cache Hit Invalid reason(s):
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
---------------------------------------------------------------------
Using Alphabet: 0abc
Using bound: 4

************************************
(declare-const a_1 String)

(assert (str.prefixof "abc" a_1))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abc"

MAS:

====================================================== Method Sequences
[test(**UNDEFINED**,**UNDEFINED**)]

====================================================== JUnit 4.0 test class
import static org.junit.Assert.*;
import org.junit.Before;
import org.junit.Test;

public class gov_nasa_jpf_symbc_strings_ExSymExeStrings09Test {

	private gov.nasa.jpf.symbc.strings.ExSymExeStrings09 gov_nasa_jpf_symbc_strings_exsymexestrings09;

	@Before
	public void setUp() throws Exception {
		gov_nasa_jpf_symbc_strings_exsymexestrings09 = new gov.nasa.jpf.symbc.strings.ExSymExeStrings09();
	}

	@Test
	public void test0() {
		gov_nasa_jpf_symbc_strings_exsymexestrings09.test(**UNDEFINED**,**UNDEFINED**);
	}
}

====================================================== results
no errors detected

====================================================== statistics
elapsed time:       00:00:00
states:             new=23,visited=0,backtracked=23,end=7
search:             maxDepth=5,constraints=0
choice generators:  thread=1 (signal=0,lock=1,sharedRef=0,threadApi=0,reschedule=0), data=8
heap:               new=499,released=67,maxLive=456,gcCycles=15
instructions:       6547
max memory:         477MB
loaded code:        classes=82,methods=1665


************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (= a_1 b_2))
(assert (not (str.prefixof "b" b_2)))
(assert (not (str.prefixof "abc" a_1)))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "aAC"
b_2 => "aAC"

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (not (= a_1 b_2)))
(assert (not (str.prefixof "b" b_2)))
(assert (str.prefixof "abc" a_1))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abc"
b_2 => "A"

MAS:
## Warning: empty path condition
### PCs: total:11 sat:11 unsat:0

string analysis: SPC # = 3
(a_1_SYMSTRING equals b_2_SYMSTRING) && (b_2_SYMSTRING notstartswith CONST_b) && (a_1_SYMSTRING startswith CONST_abc)
NPC constraint # = 0
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
Calling MAS

The smtlib query is: 
(declare-const a_1 String)
(declare-const b_2 String)

(assert (= a_1 b_2))
(assert (not (str.prefixof "b" b_2)))
(assert (str.prefixof "abc" a_1))


=======================================
Cache Hit Invalid reason(s):
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 1 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
	INVALID due to 1 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 1 multi-sym predicates, and 0 bad operations.
	INVALID due to 2 dependent symbolic variables, 0 multi-sym predicates, and 0 bad operations.
---------------------------------------------------------------------
Using Alphabet: 0abc
Using bound: 4

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (= a_1 b_2))
(assert (str.prefixof "b" b_2))
(assert (str.prefixof "abc" a_1))
------------------------------------
z3str3:
Satisfiable: false

************************************
(declare-const a_1 String)
(declare-const b_2 String)

(assert (str.prefixof "b" b_2))
(assert (not (str.prefixof "abc" a_1)))
------------------------------------
z3str3:
Satisfiable: true
a_1 => "abA"
b_2 => "b"

MAS:
string analysis: SPC # = 1
(a_1_SYMSTRING[**UNDEFINED**] notstartswith CONST_abc)
NPC constraint # = 0
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
Calling MAS

The smtlib query is: 
(declare-const a_1 String)

(assert (not (str.prefixof "abc" a_1)))


=======================================

################################################
################################################
################	CACHE HIT	################
################################################
################################################

sat,
a_1: ""

