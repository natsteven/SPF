************************************
(declare-const s1_1 String)
(declare-const out_str String)

(assert (not (= out_str "helloworld")))

(define-fun-rec toLower ((x String) (y String)) Bool (or (and (= x "") (= y "")) (and (not (= x "")) (not (= y "")) (let ((x_head (str.at x 0)) (y_head (str.at y 0)) (x_tail (str.substr x 1 (- (str.len x) 1))) (y_tail (str.substr y 1 (- (str.len y) 1)))) (and (= (str.to_code y_head) (ite (and (<= 65 (str.to_code x_head)) (<= (str.to_code x_head)                 90)) (+ (str.to_code x_head) 32) (str.to_code x_head))) (toLower x_tail y_tail))))))
(assert (toLower s1_1 out_str))
------------------------------------
z3str3:
Satisfiable: true
s1_1 => ""
out_str => ""

MAS:
## Warning: empty path condition
### PCs: total:2 sat:2 unsat:0

string analysis: SPC # = 1
(.tolowercase(s1_1_SYMSTRING) equals CONST_helloworld)
NPC constraint # = 0
$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$
Calling MAS

The smtlib query is: 
(declare-const s1_1 String)
(declare-const out_str String)

(assert (= out_str "helloworld"))

(define-fun-rec toLower ((x String) (y String)) Bool (or (and (= x "") (= y "")) (and (not (= x "")) (not (= y "")) (let ((x_head (str.at x 0)) (y_head (str.at y 0)) (x_tail (str.substr x 1 (- (str.len x) 1))) (y_tail (str.substr y 1 (- (str.len y) 1)))) (and (= (str.to_code y_head) (ite (and (<= 65 (str.to_code x_head)) (<= (str.to_code x_head)                 90)) (+ (str.to_code x_head) 32) (str.to_code x_head))) (toLower x_tail y_tail))))))
(assert (toLower s1_1 out_str))
=======================================

################################################
################################################
################	CACHE HIT	################
################################################
################################################

sat,
s1_1: "helloworld"

************************************
(declare-const s1_1 String)
(declare-const out_str String)

(assert (= out_str "helloworld"))

(define-fun-rec toLower ((x String) (y String)) Bool (or (and (= x "") (= y "")) (and (not (= x "")) (not (= y "")) (let ((x_head (str.at x 0)) (y_head (str.at y 0)) (x_tail (str.substr x 1 (- (str.len x) 1))) (y_tail (str.substr y 1 (- (str.len y) 1)))) (and (= (str.to_code y_head) (ite (and (<= 65 (str.to_code x_head)) (<= (str.to_code x_head)                 90)) (+ (str.to_code x_head) 32) (str.to_code x_head))) (toLower x_tail y_tail))))))
(assert (toLower s1_1 out_str))
------------------------------------
z3str3:
Satisfiable: true
s1_1 => "heLloWOrlD"
out_str => "helloworld"

MAS:

====================================================== Method Sequences
[test(**UNDEFINED**)]

====================================================== JUnit 4.0 test class
import static org.junit.Assert.*;
import org.junit.Before;
import org.junit.Test;

public class edu_boisestate_cs_MAS_LowerCaseTestTest {

	private edu.boisestate.cs.MAS.LowerCaseTest edu_boisestate_cs_mas_lowercasetest;

	@Before
	public void setUp() throws Exception {
		edu_boisestate_cs_mas_lowercasetest = new edu.boisestate.cs.MAS.LowerCaseTest();
	}

	@Test
	public void test0() {
		edu_boisestate_cs_mas_lowercasetest.test(**UNDEFINED**);
	}
}

====================================================== results
no errors detected

====================================================== statistics
elapsed time:       00:00:00
states:             new=5,visited=0,backtracked=5,end=2
search:             maxDepth=3,constraints=0
choice generators:  thread=1 (signal=0,lock=1,sharedRef=0,threadApi=0,reschedule=0), data=2
heap:               new=460,released=26,maxLive=452,gcCycles=4
instructions:       6357
max memory:         477MB
loaded code:        classes=81,methods=1618


